<!DOCTYPE html>
<html lang="en">
<head>
  <title>Welcome! | Erick's Website</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%#= javascript_include_tag "turbo" %>
<!--  <meta name="turbo-prefetch" content="false">-->
</head>

<body>
<div class="imagebg"></div>
<header id="site-header" data-controller="modal">
  <!-- Hamburger button -->
  <div class="hamburger-menu" data-action="click->modal#open">
    <div class="line">
      <svg xmlns="http://www.w3.org/2000/svg" x="0px" y="0px" width="100" height="100" viewBox="0,0,256,256" data-t>
        <g transform=""><g fill="#ffffff" fill-rule="nonzero" stroke="none" stroke-width="1" stroke-linecap="butt" stroke-linejoin="miter" stroke-miterlimit="10" stroke-dasharray="" stroke-dashoffset="0" font-family="none" font-weight="none" font-size="none" text-anchor="none" style="mix-blend-mode: normal"><g transform="scale(5.12,5.12)"><path d="M0,7.5v5h50v-5zM0,22.5v5h50v-5zM0,37.5v5h50v-5z"></path></g></g></g>
      </svg>
    </div>
  </div>

  <!-- Mobile menu or modal -->
  <div id="modal" data-modal-target="modal">
    <div class="modal-x" data-action="click->modal#close">X</div>
    <div class="modal-content">
      <ul>
        <li><%= link_to 'Home', root_path, class: ('active' if current_page?(root_path)) %></li>
        <li><%= link_to 'Curriculum', curriculum_path, class: ('active' if current_page?(curriculum_path)) %></li>
        <li><%= link_to 'Projects', projects_path, class: ('active' if current_page?(projects_path)) %></li>
        <li><%= link_to 'Services', services_path, class: ('active' if current_page?(services_path)) %></li>
        <li><%= link_to 'About', about_path, class: ('active' if current_page?(about_path)) %></li>
      </ul>
    </div>
  </div>

  <!-- Desktop menu -->
  <div class="full-container">
    <div class="buttons">
      <a class="btn-left" href="https://calendly.com/er-contreras/30min" target="_blank">
        ☎ Book a
        <u>Free</u>
        Consultation
      </a>
      <a class="btn-right" href="mailto:er.contreras@hotmail.com">Email me</a>
    </div>

    <ul>
      <li ><%= link_to 'Home', root_path, class: ('active' if current_page?(root_path)) %></li>
      <li ><%= link_to 'Curriculum', curriculum_path, class: ('active' if current_page?(curriculum_path)) %></li>
      <li><%= link_to 'Projects', projects_path, class: ('active' if current_page?(projects_path)) %></li>
      <li><%= link_to 'Services', services_path, class: ('active' if current_page?(services_path)) %></li>
      <li><%= link_to 'About', about_path, class: ('active' if current_page?(about_path)) %></li>
    </ul>
  </div>

  <div class="container">
    <a>Erick C. Website, <span style="color: yellow">Under Construction! 👨🏻‍💻</span> </a>
    <h1>
      Pro Developer
      <br>
      Get your custom website today!
    </h1>
  </div>
</header>

<%= yield %>
</body>
</html>
